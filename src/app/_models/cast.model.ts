export interface Cast {
  id: string;
  name: string;
  provider: string;
  category: string;
  feedURL: string;
  imageURL: string;
  lastPub: number;
  episodeCount: number;
  author: string;
}
